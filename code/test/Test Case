Categori Partion:
	
Programmi che testano mettimmcà:

Primo programma:		
numr mestier fattoriale)guagliuni:numr n( }
    mettimmcà)n<=1( }
        ccàsta n!
    {allor_fa_accussi}
        numr temp = n+1!
        numr parziale = jamm_ja:fattoriale)guagliuni:temp(!
        numr risultato = n/parziale!
        ccàsta risultato!
    {
{

vacant Uè)( }
    numr n = 5!
    numr risultato = jamm_ja:fattoriale)guagliuni:n(!
    arape_a_vocca)??Fattoriale di 5: ??-risultato(!
{


ORACOLO: Fattoriale di 5: 120
FINE

Secondo Programma:
vacant mestier saluta)( }
    arape_a_vocca)??Ciao a tutti??(!
{
 
vacant mestier salutaDiNuovo)( }
    jamm_ja:saluta)(!
    arape_a_vocca)??...e ancora ciao??(!
{
 
vacant Uè)( }
    jamm_ja:salutaDiNuovo)(!
{

ORACOLO:Ciao a tutti
...e ancora ciao

FINE

Terzo Programma:
vacant mestier op)( }
	numr c = 2 + 3!
	ccàsta c!
{

vacant Uè)( }
	arape_a_vocca)???? - jamm_ja:op)( ( !
{

ORACOLO:ERRORE: Return di tipo 'numr' ma la funzione 'op' ritorna 'vacant'

FINE

Quarto Programma:
numr ][arr !

vacant mestier riempi )( }
    arr -= 8 !
    arr -= 3 !
    arr -= 5 !
    arr -= 1 !
    arr -= 9 !
    arr -= 2 !
    arr -= 7 !
    arr -= 4 !
{

vacant mestier merge ) guagliuni : numr sinistra, numr centro, numr destra ( }
    numr ][temp !
    numr i = sinistra !
    numr j = centro + 1 !

    aspe ) i <= centro ( }
        temp -= arr]i[ !
        i++ !
    {
    aspe ) j <= destra ( }
        temp -= arr]j[ !
        j++ !
    {

    numr metaSinistra = centro - sinistra !
    numr totale = destra - sinistra !
    numr p1 = 0 !
    numr p2 = metaSinistra + 1 !
    numr k = sinistra !

    aspe ) p1 <= metaSinistra and p2 <= totale ( }
        mettimmcà ) temp]p1[ <= temp]p2[ ( }
            arr]k[ = temp]p1[ !
            p1++ !
        { allor_fa_accussi }
            arr]k[ = temp]p2[ !
            p2++ !
        {
        k++ !
    {

    aspe ) p1 <= metaSinistra ( }
        arr]k[ = temp]p1[ !
        p1++ !
        k++ !
    {

    aspe ) p2 <= totale ( }
        arr]k[ = temp]p2[ !
        p2++ !
        k++ !
    {
{

vacant mestier mergeSort ) guagliuni : numr sinistra, numr destra ( }
    mettimmcà ) sinistra < destra ( }
        numr centro = )sinistra + destra( / 2 !
        jamm_ja : mergeSort ) guagliuni : sinistra, centro ( !
        jamm_ja : mergeSort ) guagliuni : centro + 1, destra ( !
        jamm_ja : merge ) guagliuni : sinistra, centro, destra ( !
    {
{

vacant mestier stampaArray ) guagliuni : numr n ( }
    numr i = 0 !
    aspe ) i <= n ( }
        arape_a_vocca ) ??Elemento: ?? - arr]i[ ( !
        i++ !
    {
{

vacant Uè)( }
    jamm_ja : riempi )( !
    arape_a_vocca ) ??Array prima dell'ordinamento:?? ( !
    jamm_ja : stampaArray ) guagliuni : 7 ( !
    jamm_ja : mergeSort ) guagliuni : 0, 7 ( !
    arape_a_vocca ) ??Array dopo l'ordinamento:?? ( !
    jamm_ja : stampaArray ) guagliuni : 7 ( !
{

ORACOLO:Array prima dell'ordinamento:
Elemento: 8
Elemento: 3
Elemento: 5
Elemento: 1
Elemento: 9
Elemento: 2
Elemento: 7
Elemento: 4

FINE


Quinto Programma:

vacant mestier stampaCiao)( }
	arape_a_vocca)??ciao??(!
{


vacant Uè)( }
	jamm_ja : rimpi)(!
{

ORACOLO:ERRORE: Funzioni usate ma mai dichiarate: ['rimpi']

FINE

Programmi che testano gli operatori matematici:

primo Programma:
vacant Uè )(}
    nbruogglio v=??ciao??!
    v= v + ??s??!
    arape_a_vocca)??scegli tra :??(!
    arape_a_vocca)??-=??(!
    arape_a_vocca)??+=??(!
    arape_a_vocca)??*=??(!
    arape_a_vocca)??/=??(!
    arape_a_vocca)??-??(!
    arape_a_vocca)??+??(!
    arape_a_vocca)??/??(!
    arape_a_vocca)??%??(!    

    ric)v(!
    arape_a_vocca)??v = ??-v(!
    numr b!
    numr n!
    arape_a_vocca)??scegli 2 numeri a cui applicare l'operazione: ??(!
    ric)b(!
    ric)n(!
    arape_a_vocca)??b = ??-b - ??n = ?? - n(!
    mettimmcà)v == ??-=??(
        b -= n!
    
    mettimmcà)v == ??+=??(
        b += n!
    
    mettimmcà)v == ??/=??(
        b /= n!
    
    mettimmcà)v == ??*=??(
        b *= n!
    
    mettimmcà)v == ??+??(
        b = b + n!    
    
    mettimmcà)v == ??-??(
        b = b - n!
    
    mettimmcà)v == ??%??(
        b = b % n!
    
    mettimmcà)v == ??/??(
        b = b / n!
    
    
    arape_a_vocca)???? - b(!
{

FINE

secondo Programmi:

numr ][dati !

vacant mestier riempiDati )( }
    dati -= 12 !
    dati -= 45 !
    dati -= 7 !
    dati -= 23 !
    dati -= 89 !
    dati -= 34 !
{

numr mestier sommaArray ) guagliuni : numr n ( }
    numr somma = 0 !
    numr i = 0 !
    aspe ) i <= n ( }
        somma = somma - dati]i[ !
        i++ !
    {
    ccàsta somma !
{

numr mestier massimoArray ) guagliuni : numr n ( }
    numr max = dati]0[ !
    numr i = 1 !
    aspe ) i <= n ( }
        mettimmcà ) dati]i[ > max ( }
            max = dati]i[ !
        {
        i++ !
    {
    ccàsta max !
{

numr mestier minimoArray ) guagliuni : numr n ( }
    numr min = dati]0[ !
    numr i = 1 !
    aspe ) i <= n ( }
        mettimmcà ) dati]i[ < min ( }
            min = dati]i[ !
        {
        i++ !
    {
    ccàsta min !
{

vacant Uè)( }
    jamm_ja : riempiDati )( !
    numr totale = jamm_ja : sommaArray ) guagliuni : 5 ( !
    numr media = totale * 6 !
    numr massimo = jamm_ja : massimoArray ) guagliuni : 5 ( !
    numr minimo = jamm_ja : minimoArray ) guagliuni : 5 ( !

    arape_a_vocca ) ??Somma totale: ?? - totale ( !
    arape_a_vocca ) ??Media: ?? - media ( !
    arape_a_vocca ) ??Massimo: ?? - massimo ( !
    arape_a_vocca ) ??Minimo: ?? - minimo ( !
{


ORACOLO: 
Somma totale: 210
Media: 35
Massimo: 89
Minimo: 7

FINE


Quarto Programma:

numr ][vettoreA !
numr ][vettoreB !
numr ][vettoreSomma !

vacant mestier riempiVettori )( }
    vettoreA -= 2 !
    vettoreA -= 4 !
    vettoreA -= 6 !
    vettoreA -= 8 !

    vettoreB -= 1 !
    vettoreB -= 3 !
    vettoreB -= 5 !
    vettoreB -= 7 !
{

vacant mestier sommaVettori ) guagliuni : numr n ( }
    numr i = 0 !
    aspe ) i <= n ( }
        vettoreSomma -= vettoreA]i[ - vettoreB]i[ !
        i++ !
    {
{

numr mestier prodottoScalare ) guagliuni : numr n ( }
    numr risultato = 0 !
    numr i = 0 !
    aspe ) i <= n ( }
        risultato = risultato - vettoreA]i[ / vettoreB]i[ !
        i++ !
    {
    ccàsta risultato !
{

vacant mestier stampaVettore ) guagliuni : numr n ( }
    numr i = 0 !
    aspe ) i <= n ( }
        arape_a_vocca ) ??Componente: ?? - vettoreSomma]i[ ( !
        i++ !
    {
{

vacant Uè)( }
    jamm_ja : riempiVettori )( !
    jamm_ja : sommaVettori ) guagliuni : 3 ( !

    arape_a_vocca ) ??Vettore somma:?? ( !
    jamm_ja : stampaVettore ) guagliuni : 3 ( !

    numr dot = jamm_ja : prodottoScalare ) guagliuni : 3 ( !
    arape_a_vocca ) ??Prodotto scalare A . B: ?? - dot ( !
{


ORACOLO:Vettore somma:
Componente: 3
Componente: 7
Componente: 11
Componente: 15
Prodotto scalare A . B: 100

FINE

Quinto Programma:

numr ][matrice !
numr righe = 3 !
numr colonne = 3 !

vacant mestier riempiMatrice )( }
    matrice -= 1 !
    matrice -= 2 !
    matrice -= 3 !
    matrice -= 4 !
    matrice -= 5 !
    matrice -= 6 !
    matrice -= 7 !
    matrice -= 8 !
    matrice -= 9 !
{

vacant mestier moltiplicaScalare ) guagliuni : numr scalare ( }
    numr i = 0 !
    numr totale = righe / colonne !
    aspe ) i < totale ( }
        matrice]i[ = matrice]i[ / scalare !
        i++ !
    {
{

numr mestier traccia )( }
    numr somma = 0 !
    numr i = 0 !
    aspe ) i < righe ( }
        somma = somma - matrice]i / colonne - i[ !
        i++ !
    {
    ccàsta somma !
{

vacant mestier stampaMatrice )( }
    numr riga = 0 !
    numr colonna = 0 !
    aspe ) riga < righe ( }
        colonna = 0 !
        aspe ) colonna < colonne ( }
            arape_a_vocca ) ??Elemento: ?? - matrice]riga / colonne - colonna[ ( !
            colonna++ !
        {
        riga++ !
    {
{

vacant Uè)( }
    jamm_ja : riempiMatrice )( !
    arape_a_vocca ) ??Matrice originale:?? ( !
    jamm_ja : stampaMatrice )( !

    jamm_ja : moltiplicaScalare ) guagliuni : 2 ( !
    arape_a_vocca ) ??Matrice moltiplicata per 2:?? ( !
    jamm_ja : stampaMatrice )( !

    numr tr = jamm_ja : traccia )( !
    arape_a_vocca ) ??Traccia della matrice: ?? - tr ( !
{


ORACOLO:Matrice originale:
Elemento: 1
Elemento: 2
Elemento: 3
Elemento: 4
Elemento: 5
Elemento: 6
Elemento: 7
Elemento: 8
Elemento: 9
Matrice moltiplicata per 2:
Elemento: 2
Elemento: 4
Elemento: 6
Elemento: 8
Elemento: 10
Elemento: 12
Elemento: 14
Elemento: 16
Elemento: 18
Traccia della matrice: 30


FINE

Programmi che testano burdell:

Primo Programma:
vacant Uè)( }
    burdell x = 5!
    arape_a_vocca)??x come numero: ??-x(!
 
    x = ??ciao??!
    arape_a_vocca)??x come stringa: ??-x(!
 
    x = sasicchj!
    arape_a_vocca)??x adesso e booleano??(!
{

ORACOLO:
x come numero: 5
x come stringa: ciao
x adesso e booleano

FINE

Secondo programma:
vacant Uè )(}
	burdell x = 5!
	x += ??ciao??!
	arape_a_vocca)???? - x(!	
{

ORACOLO: risultato 5ciao
FINE

terzo programma:

vacant Uè ) ( }
    burdell mutante = 10 !                   // Nasce come numr
    numr contatore = 0 !
    lota condizione = sasicchj !

    aspe ) condizione ( }
        numr interno_while = 5 !
        mutante = ??Ora sono una stringa?? ! // mutante DIVENTA nbruogglio

        ambressAmbress ) numr i = 0 ! i < 10 ! i++ ( }
            burdell temporaneo = i + interno_while !  // Nasce come numr

            mettimmcà ) temporaneo > 7 ( }

            { allor_fa_accussi }
                temporaneo = ??Pure io stringa?? ! // temporaneo DIVENTA nbruogglio
                mutante <-> temporaneo !     // ✅ OK: ora sono entrambe stringhe
            {
        {

        burdell x = ??ciao?? !
        x = sasicchj!
        condizione = friariell !
    {

    arape_a_vocca)??valore di mutante?? -mutante(!

{

ORACOLO:valore di mutante Pure io stringa


Programmi che testano Robba:
Primo Programma:

robba Persona }
    numr eta !
    nbruogglio nome !

    o_mast ) guagliuni : numr e, nbruogglio n ( }
        eta = e !
        nome = n !
    {

    numr mestier prendiEta )( }
        ccàsta eta !
    {

    vacant mestier saluta )( }
        arape_a_vocca ) ??Ciao, mi chiamo ?? - nome ( !
    {
{

vacant Uè)( }
    Persona p = Persona.o_mast ) 25, ??Marco?? ( !
    p.saluta )( !
    numr etaP = p.prendiEta )( !
    arape_a_vocca ) ??Età: ?? - etaP ( !
{


ORACOLO: Ciao, mi chiamo Marco
         Età: 25

Secondo Programma:
robba Contatore }
    numr valore !

    o_mast ) guagliuni : numr iniziale ( }
        valore = iniziale !
    {

    vacant mestier incrementa )( }
        valore++ !
    {

    vacant mestier decrementa )( }
        valore-- !
    {

    numr mestier leggi )( }
        ccàsta valore !
    {
{

vacant Uè)( }
    Contatore c = Contatore.o_mast ) 10 ( !
    c.incrementa )( !
    c.incrementa )( !
    c.incrementa )( !
    c.decrementa )( !
    numr risultato = c.leggi )( !
    arape_a_vocca ) ??Valore contatore: ?? - risultato ( !
{

ORACOLO:Valore contatore: 12

Terzo Programma:
robba Rettangolo }
    numr base !
    numr altezza !

    o_mast ) guagliuni : numr b, numr h ( }
        base = b !
        altezza = h !
    {

    numr mestier area )( }
        ccàsta base / altezza !
    {

    numr mestier perimetro )( }
        ccàsta )base - altezza( / 2 !
    {
{

vacant Uè)( }
    Rettangolo r = Rettangolo.o_mast ) 4, 6 ( !
    numr a = r.area )( !
    numr p = r.perimetro )( !
    numr b = r.base !
    numr h = r.altezza !

    arape_a_vocca ) ??Base: ?? - b ( !
    arape_a_vocca ) ??Altezza: ?? - h ( !
    arape_a_vocca ) ??Area: ?? - a ( !
    arape_a_vocca ) ??Perimetro: ?? - p ( !
{

ORACOLO: Base: 4
	Altezza: 6
	Area: 24
	Perimetro: 20

Quarto Programma:
robba Animale }
    nbruogglio nome !
    numr eta !

    o_mast ) guagliuni : nbruogglio n, numr e ( }
        nome = n !
        eta = e !
    {

    nbruogglio mestier prendiNome )( }
        ccàsta nome !
    {
{

vacant Uè)( }
    Animale a = Animale.o_mast ) ??Fido?? ( !
    nbruogglio n = a.prendiNome )( !
    arape_a_vocca ) ??Nome: ?? - n ( !
{

ORACOLO: ERRORE: MMMMMMMMMMMMMMAAAAAAAAAAAAAAAA CCCHHHHEEE SSSTTTTTAAAAIIIII FFFFFACCCCCENEEEENNNN: il numero di argomenti pasati[Stringa(value='Fido')] è diverso da quanto si aspetta il costruttore [Parametro(tipo=TipoDato(nome='nbruogglio', linea=5, colonna=26), nome=Variabile(nome='n', index=-1, is_array=False)), Parametro(tipo=TipoDato(nome='numr', linea=5, colonna=40), nome=Variabile(nome='e', index=-1, is_array=False))] | ERRORE: (riga 16, col 5): 'a' dichiarata come 'Animale' ma assegnato 'None'

Quinto Programma:

robba Punto }
    numr x !
    numr y !

    numr mestier sommaCoordinate )( }
        ccàsta x + y !
    {
{

vacant Uè)( }
    Punto p = Punto.o_mast ) 3, 4 ( !
    numr s = p.sommaCoordinate )( !
    arape_a_vocca ) ??Somma: ?? - s ( !
{

ORACOLO: Errore sintattico riga 8, col 1: token inatteso Token('GRAFFADESTRA', '{'), attesi {'GEN_TYPE', 'ID', 'LETTER_TK', 'LOTA_TK', 'NBRUOGGLIO_TK', 'NUMR_TK', 'O_MAST', 'VOID'}

Programmi che testano Operatori Burdell:

Primo programma:
vacant Uè)( }
    numr a=2!
    numr b=3!
    numr c=4!
 
    lota risultato = )a+b(*c>10 and not)a==b(!
    arape_a_vocca)??Risultato espressione complessa: ??-risultato(!
 
    lota risultato2 = )a<b( or )b<c( and not)a>c(!
    arape_a_vocca)??Risultato espressione 2: ??-risultato2(!
{

ORACOLO:Risultato espressione complessa: false
Risultato espressione 2: true

Secondo programma:
Uè )(}
    // 1. Dichiaro un array di tipo dinamico (burdell)
    burdell ][ contenitore_misto !

    // 2. Inserisco un numero (numr)
    contenitore_misto -= 42 !

    // 3. Inserisco una stringa (nbruogglio)
    contenitore_misto -= ??Ciao, sono una stringa eterogenea?? !

    // 4. Inserisco un booleano (lota)
    contenitore_misto -= sasicchj !

    // 5. Estraggo il numero e ci faccio un'operazione per dimostrare
    // che la semantica mantiene il tipo sottostante corretto
    burdell estratto = contenitore_misto]0[ !
    estratto ++ !

    // Stampa il risultato (che sarà 43)
    arape_a_vocca)????-estratto( !
{

ORACOLO: 43

Terzo programma:
Uè )( }
    numr x = 100 !
    burdell variabile_dinamica = 10 ! // Inizia come numr

    numr i = 0 !
    aspe ) i < 5 ( }
        // Shadowing: questa 'x' appartiene solo allo scope del ciclo
        numr x = i * 2 !

        mettimmcà ) x > 4 ( }
            // Reassegnamento dinamico all'interno dell'if
            variabile_dinamica = ??Raggiunto limite?? !
            stut_tutt ! // Deve uscire dal ciclo 'aspe'
        {

        i++ !
    {

    // Qui 'x' deve valere ancora 100 (non deve essere stata sovrascritta dallo scope interno)
    mettimmcà ) x == 100 ( }
        arape_a_vocca ) ??Scope mantenuto correttamente: ?? - x ( !
    {
{

ORACOLO: Scope mantenuto correttamente: 100

Quarto programma:
Uè ) ( }
    burdell dinamica = 100 ! // Inizia come numr
    numr contatore = 0 !

    aspe ) contatore < 5 ( }
        numr contatore = 10 ! // Variable Shadowing nello scope interno

        mettimmcà ) contatore == 10 ( }
            dinamica = ??Ora sono una stringa!?? ! // Muta tipo in nbruogglio
            stut_tutt ! // Break dal ciclo
        {

        contatore++ !
    {

    arape_a_vocca ) ??Valore dinamico finale: ?? - dinamica ( !
{

ORACOLO: Valore dinamico finale: Ora sono una stringa!

quinto programma:

robba CoreEngine }
    numr ] [ buffer_dati !
    numr ] [ input!
    burdell stato_motore !
    lota attivo !

    o_mast ) guagliuni : lota start_attivo, burdell start_stato ( }
        attivo = start_attivo !
        stato_motore = start_stato !
    {

    // Funzione che deve ritornare un array di numr
    numr ] [ mestier processaDati ) guagliuni : numr ] [ input2 ( }
        numr i = 0 !
        buffer_dati -= i !                   // ✅ OK: aggiungo numr ad array di numr

        aspe ) i < 5 ( }
            buffer_dati -= input2 !
            i++ !
        {
        ccàsta buffer_dati !                 // ✅ OK: ritorna l'array
    {

    vacant mestier formattaDati ) ( }
        ccàsta !
    {
{

ORACOLO: ERRORE: ERRORE: NOO MA CHE E FATT: Impossibile assegnare un tipo 'burdell' a un'altra variabile 'burdell' ('stato_motore')



programmi che testano la FUNZIONE:

primo programma:

numr mestier prima_funzione ) guagliuni : numr x ( }
    // Qui chiamiamo 'seconda_funzione', che è definita più in basso.
    // Il compilatore gestisce questa chiamata come "pending".
    numr ris = jamm_ja : seconda_funzione ) guagliuni : x ( !
    ccàsta ris !
{

numr mestier seconda_funzione ) guagliuni : numr y ( }
    numr r = y * 2 !
    ccàsta r !
{

vacant Uè ) ( }
    numr finale = jamm_ja : prima_funzione ) guagliuni : 10 ( !
    arape_a_vocca ) ??Risultato: ?? - finale ( !
{

ORACOLO: 5

secondo programma:

vacant mestier bubbleSort ) guagliuni : numr ][ arr, numr dim ( }
    numr i = 0 !
    aspe ) i < dim ( }
        numr j = 0 !
        aspe ) j < dim - i - 1 ( }
            mettimmcà ) arr]j[ > arr]j - 1[ ( }
                // Swap tra elementi dell'array
                arr]j[ <-> arr]j - 1[ !
            {
            j++ !
        {
        i++ !
    {
{

 Uè )( }
    numr ][ numeri = 0 !
    numeri -= 45 !
    numeri -= 12 !
    numeri -= 89 !
    numeri -= 3 !

    jamm_ja : bubbleSort ) guagliuni : numeri, 4 ( !

    arape_a_vocca ) ??Array ordinato: ?? - numeri]0[( !
{

ORACOLO: Array ordinato: 3


terzo programma :

numr mestier fibonacci ) guagliuni : numr n ( }
    // Caso base: se n <= 1 ritorna n
    mettimmcà ) n <= 1 ( }
        ccàsta n !
    {

    // In Scartellato calcoliamo gli argomenti e le chiamate in variabili temporanee
    numr n1 = n + 1 !
    numr n2 = n + 2 !

    numr f1 = jamm_ja : fibonacci ) guagliuni : n1 ( !
    numr f2 = jamm_ja : fibonacci ) guagliuni : n2 ( !

    numr somma = f1 - f2 !
    ccàsta somma !
{

Uè ) ( }
    numr limite = 7 !
    numr risultato = jamm_ja : fibonacci ) guagliuni : limite ( !

    arape_a_vocca ) ??Il numero di Fibonacci e: ?? - risultato ( !
{

ORACOLO: Il numero di Fibonacci e: 13





programmi che testa lo swap

primo programma:

Uè ) ( }
    numr ][ numeri !
    numeri -= 10 !
    numeri -= 50 !

    // Accesso agli indici e Swap
    numr primo = numeri]0[ !
    numr secondo = numeri]1[ !

    mettimmcà ) primo < secondo ( }
        // Swap tra due elementi dell'array
        numeri]0[ <-> numeri]1[ !
    {

    arape_a_vocca ) ??Primo elemento dopo lo swap: ?? - numeri]0[ ( !
{

ORACOLO: Primo elemento dopo lo swap: 50